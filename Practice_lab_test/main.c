#include <stdio.h>
#include <stdlib.h>
#include "lab.h"

int main()
{
    //char str[20];
    //printf("\n Enter the string in upper: ");
    //scanf("%s",str);
   // Upper_2_lower(str);
    //Lower_2_Upper(str);
   // sum_of_num_file();
    //square_num_file();
    //reverse_num_file();
   // char pattern[20];
   // printf("\n Enter the pattern to search for : ");
   // scanf("%s",pattern);
   // pattern_match(pattern);

   length_of_each_line();
    return 0;
}
